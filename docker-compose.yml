services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile.api
    # container_name: sznportal_api
    env_file:
      - .env # Load environment variables from the .env file
    ports:
      - "3333:3333" # Expose the API on port 3333
    networks:
      - sznportal_network

  web:
    build:
      context: .
      dockerfile: ./Dockerfile.web
    # container_name: sznportal_web
    ports:
      - "3000:3000" # Expose the web app on port 3000
    networks:
      - sznportal_network
    environment:
      - API_URL=http://api:3333/search 

  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro                
      # - ./nginx/api_key_map.conf:/etc/nginx/conf.d/api_key_map.conf:ro  
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro    
      - ./certbot/conf:/etc/letsencrypt                          
      - ./certbot/www:/var/www/certbot                           
    depends_on:
      - web

  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    command: certonly --webroot -w /var/www/certbot --force-renewal --email johngoyason@gmail.com -d schnauzenportal.de --agree-tos

networks:
  sznportal_network:
    driver: bridge
